<p>
  <strong>Project:</strong>
  <%= @project.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @project.description %>
</p>

<% (0...@project.images.count).each do |image| %>
  <%= image_tag(@project.thumbnail(image)) %>
<% end %>

<br>
<% if current_user %>
<%= link_to 'Edit', edit_admin_project_path(@project) %> |
<% end %>
<%= link_to 'Back', projects_path %>
